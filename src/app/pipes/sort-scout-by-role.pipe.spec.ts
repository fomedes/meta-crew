import { SortScoutByRolePipe } from './sort-scout-by-role.pipe';

describe('SortScoutByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SortScoutByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
