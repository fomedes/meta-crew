import { SpecificRolePipe } from './specific-role.pipe';

describe('SpecificRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SpecificRolePipe();
    expect(pipe).toBeTruthy();
  });
});
