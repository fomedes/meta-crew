import { SortByRolePipe } from './sort-by-role.pipe';

describe('SortByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
