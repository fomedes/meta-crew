import { FormatStreakPipe } from './format-streak.pipe';

describe('FormatStreakPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStreakPipe();
    expect(pipe).toBeTruthy();
  });
});
